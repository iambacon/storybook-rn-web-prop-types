# Storybook with react-native-web and working prop types

An example of the storybook react-native-web addon displaying prop type information
correctly.

## Setup

`yarn install`

## Usage

Run storybook with `yarn storybook`
